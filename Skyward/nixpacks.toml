buildImage = "ubuntu:24.04"
[phases.setup]
aptPkgs = ["python3-pip", "ffmpeg", "xvfb"]
[phases.install]
cmds = ["pip3 install -r requirements.txt"]
[phases.build]
cmds = ["playwright install chromium"]
